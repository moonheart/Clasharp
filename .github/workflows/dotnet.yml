name: Build

env:
  ProjectName: clasharp

on:
  push:
    branches: [ master ]
    
jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        configuration: [ Debug]
        os: [ ubuntu-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup .NET 6
        uses: actions/setup-dotnet@v3.0.2
        with:
          dotnet-version: 6.0.x

      - name: Verify dotnet info
        run: dotnet --info

      - name: Publish for ${{ matrix.configuration }}
        shell: bash
        run: chmod +x ./publish.sh && ./publish.sh "${{ matrix.configuration }}"

      - name: Upload Windows App ${{ matrix.configuration }}
        continue-on-error: true
        uses: actions/upload-artifact@v3
        with:
          name: '${{ env.ProjectName }}-win-x64-${{ matrix.configuration }}'
          path: ./${{ env.ProjectName }}-win-x64-${{ matrix.configuration }}.zip
          
      - name: Upload Linux App ${{ matrix.configuration }}
        continue-on-error: true
        uses: actions/upload-artifact@v3
        with:
          name: '${{ env.ProjectName }}-linux-x64-${{ matrix.configuration }}'
          path: ./${{ env.ProjectName }}-linux-x64-${{ matrix.configuration }}.zip
          
